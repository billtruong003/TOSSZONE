# P0 1.3.1 — ShotClaim + victim-side HitValidator — Solo Injection Matrix (2026-07-14)

Scene: `Assets/_Game/Scenes/02_FSPMAP.unity`, editor Play Mode, solo (1 client, local avatar = shooter = victim).
Seam: `AvatarWeaponSync.ProcessShotClaim(...)` (public test seam, guarded off-authority) — đúng entry mà `RPC_SubmitShotClaim` gọi.
Setup: `AvatarWeaponSync.LocalEquippedWeaponId = 0` → FUN replicate `EquippedSlot=0` (AK74_GunConfig: range=40, rpm=600, catalog chỉ có id 0). Origin chuẩn lấy đúng như validator: `ResolveAnchor()` (reflection) → wrist position.
Constants (AvatarWeaponSync.cs): `OriginToleranceMeters=3`, `RangeMarginFactor=1.1` → range cutoff 44 m. Fire-rate limit = ceil(600/60 × 1.5) = 15 accept/giây/shooter.

## §4 Rejection matrix — kết quả

| # | Case (shotId) | Inject | Expected | Actual | Pass |
|---|---|---|---|---|---|
| 1 | Valid body 5 m (9001) | origin=wrist, hit=+5 m fwd, part=Body | Accept đúng 1 lần, dmg resolve victim-side | ACCEPT dmg=16, head=False, hb=5 ha=5 | ✅ |
| 2 | Replay accepted (9001) | same shotId | Duplicate | REJECT Duplicate | ✅ |
| 3 | Bad weapon (9002) | weaponId=250 | InvalidWeapon | REJECT InvalidWeapon | ✅ |
| 4 | Out-of-range (9003) | hit 60 m > 44 m cutoff | OutOfRange | REJECT OutOfRange | ✅ |
| 5 | Bad origin (9006) | origin lệch 10 m > 3 m tolerance | InvalidOrigin | REJECT InvalidOrigin | ✅ |
| 6 | Bad hit part (9007) | hitPart=7 | InvalidHitPart | REJECT InvalidHitPart | ✅ |
| 7 | Bad shooter (9008) | shooter=PlayerRef.None | InvalidShooter | REJECT InvalidShooter | ✅ |
| 8 | Spawn-protected (9004) | InvulnTimer=30 s (reflection, restore sau case) | SpawnProtected | REJECT SpawnProtected | ✅ |
| 9 | Dead victim (9005) | Health=0 (restore sau case) | InvalidShooter — shooter==victim solo nên check InvalidShooter (shooterCombat.Health<=0) chặn TRƯỚC VictimDead, đúng "first failing check wins" | REJECT InvalidShooter | ✅* |
| 10 | Over fire-rate (9100–9120) | 21 valid claims cùng frame (slot 1 đã dùng bởi 9001) | 14 accept rồi FireRate | 14× ACCEPT (9100–9113) + 7× REJECT FireRate (9114–9120); tổng accept trong window = 15 = limit | ✅ |
| 11 | Replay of REJECTED (9003) | re-inject sau khi bị OutOfRange | Duplicate (rejected cũng vào `_seenClaims`) | REJECT Duplicate | ✅ |

Tổng: 30 events lần 1 (15 accept / 15 reject) + 1 Duplicate lần 2. Mỗi claim đúng 1 event, không có accepted-result lần hai.

## Zero-Health-write check (acceptance 1.3.1)

`hpStart=5, hpEnd=5` qua toàn bộ matrix; ClaimAccepted mang `HealthBefore=HealthAfter=5` (OBSERVED only — Health write thuộc 1.3.2, blocked theo D3). Case 8/9 mutate InvulnTimer/Health chỉ để dựng điều kiện test và đã restore trong cùng frame.

## NOT covered solo (nợ lại)

- RPC transport path 2-client thật (`RPC_SubmitShotClaim`, shooter identity từ `info.Source`) — chờ two-client runbook (cùng nợ với 1.2.x).
- `EquippedMismatch`: catalog hiện chỉ có weapon id 0 → mọi id≠0 fail `InvalidWeapon` trước; cần catalog ≥2 gun để exercise.
- `VictimDead` trực tiếp: solo shooter==victim nên `InvalidShooter` precedence luôn thắng; cần 2 client.
- `CombatClosed`: scene 02_FSPMAP không có ArenaManager (combat open theo convention GunInput.cs:60).
